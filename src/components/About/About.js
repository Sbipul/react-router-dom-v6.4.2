import React from 'react';
import Navbar from '../Navbar/Navbar';

const About = () => {
    return (
        <div>
            <Navbar/>
            this is about page
        </div>
    );
};

export default About;